const fs = require('fs');

console.log(fs.readFileSync('content.txt', 'utf8'));

// hints
readFile('file name', 'encoding', callback)
const fs = require('fs/promises')
readFileSync('file name', 'encoding')



